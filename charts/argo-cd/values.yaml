argo-cd:
  dex:
    enabled: false
  server:
    spec:
      namespace: argo-cd
    extraArgs:
      - --insecure
    config:
      repositories: |
        - type: helm
          name: argo-cd
          url: https://argoproj.github.io/argo-helm
